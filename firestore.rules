rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read and write access for development and syncing user profiles/messages
      allow read, write: if true;
    }
  }
}